dependencies {
    //Nada externo, solo librerias utilitarias
}

pitest {
    threads = 1
    outputFormats = ['XML', 'HTML']
    mutationThreshold = 10
    timestampedReports = true
    timeoutConstInMillis = 10000
    enableDefaultIncrementalAnalysis = true
    pitestVersion = '1.4.6'
}